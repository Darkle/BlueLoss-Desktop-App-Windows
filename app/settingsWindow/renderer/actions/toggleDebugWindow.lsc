import { ipcRenderer } from 'electron'

export default toggleDebugWindow(element) -> state ->
  toggled = element.currentTarget.toggled
  ipcRenderer.send('renderer:user-debug-toggled', toggled)
  {...state, ...{ userDebug: toggled }}
